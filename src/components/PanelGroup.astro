---
export interface Props {
  /**
   * Direction of panels
   */
  dir?: "row" | "col"
}
---

<div class:list={["panel-group", Astro.props.dir]}>
  <slot />
</div>

<style lang="scss">
  .panel-group {
    @apply grid;
    @apply w-full h-full;

    &.row {
      grid-template-rows: repeat(auto-fill, max-content);
    }

    &.col {
      grid-template-columns: repeat(auto-fill, 50%);
    }
  }
</style>